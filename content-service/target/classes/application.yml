
server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/contentdb
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
